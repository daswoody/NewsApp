# Postgres password (also used in DATABASE_URL inside docker-compose.yml)
POSTGRES_PASSWORD=change-me

# Public URL of the app (the domain your reverse proxy serves)
ORIGIN=https://news.example.com

# Set to "false" once your accounts exist to block new sign-ups
ALLOW_REGISTRATION=true
